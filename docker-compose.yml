services:
  verdaccio:
    build:
      context: ./docker/verdaccio
    container_name: tp-cd-delivery-verdaccio
    volumes:
      - verdaccio-storage:/verdaccio/storage
    networks:
      - cd-network

  registry:
    image: registry:2
    container_name: tp-cd-delivery-registry
    volumes:
      - registry-storage:/var/lib/registry
    networks:
      - cd-network

networks:
  cd-network:
    driver: bridge

volumes:
  verdaccio-storage:
  registry-storage:
